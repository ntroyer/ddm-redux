import React, { Component } from 'react';
import styled from 'styled-components';

export default class PolyominoSelector extends Component {

    render() {
        return (
            <div>Polyomino Selector</div>
        );
    }
}